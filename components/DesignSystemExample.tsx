export default function DesignSystemExample() {
  return (
    <section aria-label="Design system usage" className="section-pad bg-bg-secondary">
      <div className="container-shell border border-border p-8">
        <h2 className="type-h2 text-text-primary">Strategic systems, built for precision.</h2>
        <p className="type-body-lg mt-4 max-w-3xl text-text-secondary">
          Our design language combines clear hierarchy, measured motion, and strong spacing to ensure
          every interface decision supports business clarity.
        </p>
      </div>
    </section>
  );
}
